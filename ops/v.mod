Module {
	name: 'mlx_ops'
	description: 'mlx-v compute layer: ergonomic array ops, linalg, conv/fft/fast/random/transforms, io. Split out so CPU-only consumers can import the light mlx core.'
	version: '0.1.0'
	license: 'MIT'
	dependencies: ['mlx']
}
